import { Dispatcher } from 'flux';

/**
 * Global Dispatcher (like a singleton)
 *
 * Documentation about the Flux architecture can be found here:
 * https://github.com/facebook/flux
 */
module.exports = new Dispatcher();